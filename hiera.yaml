---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: 'Secrets (not committed — copy secrets.yaml.example)'
    path: 'secrets.yaml'

  - name: 'Common data'
    path: 'common.yaml'
